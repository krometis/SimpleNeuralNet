#define some function stubs
#users will need to define these to run their stuff, but
#we need the names in the namespace

nnAct(z) = error("You must define the activation function nnAct().");
nnDAct(z) = error("You must define the activation function derivative nnDAct().");
nnCost(datay,y) = error("You must define the cost function nnCost(dataOutput,output).");
nnDCost(datay,y) = error("You must define the cost function derivative nnDCost(dataOutput,output).");

